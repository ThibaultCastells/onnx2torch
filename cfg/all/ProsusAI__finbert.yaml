inputs:
  path: ../onnx_shape_inference/data/shape_inferred/ProsusAI__finbert
  recursive: true
  pattern: '*.onnx'
output_dir: data/executorch/ProsusAI__finbert
conversion:
  save_input_names: false
  attach_onnx_mapping: false
device: cpu
