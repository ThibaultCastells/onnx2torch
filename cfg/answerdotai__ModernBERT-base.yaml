inputs:
  path: ../onnx_shape_inference/data/shape_inferred/answerdotai__ModernBERT-base
  recursive: true
  pattern: '*.onnx'
output_dir: data/executorch/answerdotai__ModernBERT-base
conversion:
  save_input_names: false
  attach_onnx_mapping: false
device: cpu
