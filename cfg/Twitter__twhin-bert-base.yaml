inputs:
  path: ../onnx_shape_inference/data/shape_inferred/Twitter__twhin-bert-base
  recursive: true
  pattern: '*.onnx'
output_dir: data/executorch/Twitter__twhin-bert-base
conversion:
  save_input_names: false
  attach_onnx_mapping: false
device: cpu
